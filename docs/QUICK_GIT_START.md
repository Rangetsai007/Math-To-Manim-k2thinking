# Git 推送快速开始指南 ⚡

## 🚀 5 分钟学会 Git Push

### 第一步：配置你的身份（只需一次）

```bash
# 设置你的名字
git config --global user.name "你的名字"

# 设置你的邮箱
git config --global user.email "your.email@example.com"

# 检查配置是否成功
git config --list
```

### 第二步：创建远程仓库

#### 选项 A：在 GitHub 创建
1. 访问 [GitHub.com](https://github.com)
2. 点击右上角的 `+` → `New repository`
3. 输入仓库名称（如：`Math-To-Manim`）
4. 点击 `Create repository`
5. **复制仓库地址**（如：`https://github.com/username/Math-To-Manim.git`）

#### 选项 B：在 Gitee 创建（国内速度快）
1. 访问 [Gitee.com](https://gitee.com)
2. 点击右上角的 `+` → `新建仓库`
3. 填写仓库信息
4. 点击 `创建`
5. **复制仓库地址**

### 第三步：连接远程仓库

```bash
# 进入你的项目目录
cd /Users/cailangri/Documents/claudecode/Math-To-Manim-main

# 连接远程仓库（替换成你的仓库地址）
git remote add origin https://github.com/你的用户名/仓库名.git

# 验证连接
git remote -v
```

### 第四步：首次推送

```bash
# 添加所有文件
git add .

# 提交
git commit -m "初始提交：添加项目文件"

# 推送到远程
git push -u origin main
```

---

## 📝 日常使用（每天都会用到）

### 标准流程（3 步）

```bash
# 1️⃣ 添加更改
git add .

# 2️⃣ 提交更改
git commit -m "今天做了什么：如修复 bug、添加新功能"

# 3️⃣ 推送到远程
git push
```

### 查看状态

```bash
# 查看哪些文件被修改了
git status

# 查看具体修改内容
git diff
```

---

## 🎨 在 Cursor 中使用（可视化）

### 方法一：使用源代码管理面板

1. **打开源代码管理**
   - 快捷键：`Cmd + Shift + G`（Mac）
   - 或点击左侧边栏的源代码管理图标

2. **暂存更改**
   - 点击文件旁的 `+` 号
   - 或点击顶部的 `+` 暂存所有更改

3. **提交**
   - 在顶部输入框输入提交信息
   - 按 `Cmd + Enter` 或点击 ✓ 按钮

4. **推送**
   - 点击底部状态栏的同步按钮 ↻
   - 或点击 `...` → `推送`

### 方法二：使用命令面板

1. 按 `Cmd + Shift + P` 打开命令面板
2. 输入 `Git: Add All`（添加所有文件）
3. 输入 `Git: Commit`（提交）
4. 输入 `Git: Push`（推送）

---

## 💡 常见场景

### 场景 1：修改了一些文件，想推送

```bash
git add .
git commit -m "修改：更新了配置文件"
git push
```

### 场景 2：新增了文件，想推送

```bash
git add .
git commit -m "新增：添加了数据处理模块"
git push
```

### 场景 3：删除了文件，想推送

```bash
git add .
git commit -m "删除：移除了旧的测试文件"
git push
```

### 场景 4：多人协作，先拉取再推送

```bash
# 先拉取最新代码
git pull

# 解决可能的冲突后，再推送
git add .
git commit -m "合并：整合团队最新代码"
git push
```

---

## ⚠️ 推送前检查

```bash
# ✅ 检查 1：确认要推送的文件
git status

# ✅ 检查 2：查看具体修改内容
git diff

# ✅ 检查 3：查看提交历史
git log --oneline -5
```

---

## 🔥 最常遇到的问题

### 问题 1：提示没有远程仓库

```bash
# 错误信息
fatal: No configured push destination.

# 解决方法：添加远程仓库
git remote add origin https://github.com/username/repo.git
```

### 问题 2：推送被拒绝

```bash
# 错误信息
! [rejected] main -> main (fetch first)

# 解决方法：先拉取再推送
git pull origin main
git push
```

### 问题 3：需要输入用户名密码

```bash
# GitHub 从 2021 年起不再支持密码登录
# 需要使用 Personal Access Token

# 生成 Token：
# GitHub → Settings → Developer settings → 
# Personal access tokens → Generate new token

# 推送时：
# Username: 你的 GitHub 用户名
# Password: 粘贴刚才生成的 Token（不是你的密码）
```

### 问题 4：分支名称错误

```bash
# 如果提示 master 不存在，使用 main
git push -u origin main

# 或者重命名分支
git branch -M main
git push -u origin main
```

---

## 🎯 提交信息规范

### 好的提交信息示例

```bash
git commit -m "功能：添加用户登录功能"
git commit -m "修复：解决数据导出报错问题"
git commit -m "优化：提升页面加载速度 30%"
git commit -m "文档：更新 API 使用说明"
git commit -m "重构：重构了数据处理模块"
git commit -m "样式：调整按钮间距和颜色"
git commit -m "测试：添加单元测试"
```

### 不好的提交信息示例

```bash
git commit -m "更新"  ❌ 太模糊
git commit -m "修改"  ❌ 没说明修改了什么
git commit -m "aaa"   ❌ 无意义
git commit -m "fix"   ❌ 没说明修复了什么
```

---

## 📊 工作流程图

```
┌─────────────────┐
│   修改代码      │
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│   git add .     │  ← 添加到暂存区
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│   git commit    │  ← 提交到本地仓库
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│   git push      │  ← 推送到远程仓库
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│   GitHub/Gitee  │  ← 远程仓库更新
└─────────────────┘
```

---

## 🛠️ 实用命令速查

| 命令 | 作用 | 使用频率 |
|------|------|---------|
| `git status` | 查看状态 | ⭐⭐⭐⭐⭐ |
| `git add .` | 添加所有更改 | ⭐⭐⭐⭐⭐ |
| `git commit -m "说明"` | 提交更改 | ⭐⭐⭐⭐⭐ |
| `git push` | 推送到远程 | ⭐⭐⭐⭐⭐ |
| `git pull` | 拉取远程更改 | ⭐⭐⭐⭐ |
| `git log` | 查看提交历史 | ⭐⭐⭐ |
| `git diff` | 查看具体修改 | ⭐⭐⭐ |
| `git remote -v` | 查看远程仓库 | ⭐⭐ |

---

## 🎁 实用技巧

### 技巧 1：一次性完成 add 和 commit

```bash
# 普通方式
git add .
git commit -m "提交信息"

# 快捷方式（只对已跟踪的文件有效）
git commit -am "提交信息"
```

### 技巧 2：查看简洁的状态

```bash
git status -s
```

### 技巧 3：撤销上一次提交（保留更改）

```bash
git reset --soft HEAD~1
```

### 技巧 4：查看漂亮的提交历史

```bash
git log --oneline --graph --all
```

### 技巧 5：忽略不需要推送的文件

创建 `.gitignore` 文件：

```bash
# Python
__pycache__/
*.py[cod]
*.so
.Python
venv/
*.egg-info/

# macOS
.DS_Store

# IDE
.vscode/
.idea/
*.swp

# 配置文件（可能包含密钥）
.env
config.ini
```

---

## 🔗 推荐学习资源

- 📺 [Git 入门视频教程（B站）](https://www.bilibili.com/video/BV1FE411P7B3/)
- 📖 [Git 简明指南](http://rogerdudler.github.io/git-guide/index.zh.html)
- 🎮 [可视化学习 Git](https://learngitbranching.js.org/?locale=zh_CN)
- 📚 [Git 官方中文文档](https://git-scm.com/book/zh/v2)

---

## 💬 记住这几点

1. ✅ **经常提交**：完成一个小功能就提交
2. ✅ **写清楚提交信息**：别人能看懂你改了什么
3. ✅ **每天推送**：推送就是最好的备份
4. ✅ **多用 `git status`**：时刻了解当前状态
5. ✅ **不怕犯错**：大部分操作都可以撤销

---

## 🆘 需要帮助？

如果遇到问题：
1. 复制错误信息
2. 使用 `git status` 查看状态
3. 搜索错误信息或询问 AI
4. 查看详细指南：`docs/GIT_PUSH_GUIDE.md`

祝你 Git 使用愉快！🎉

